* ゲッターとセッターを使っているので、public String name;とpublic int ageの  
publicをprivateにする。
* super()のsuperをthisにする。
* コンストラクタは戻り値がないのでvoidはいらない。
* name=nameとage=ageをthis.name=nameとthis.age=ageにする。
* nameのセッターの戻り値はvoidにする。
* $sと$dの$は%にする。